let personObject = {
    name:"God",
    age:40,
    occupation:"Overlord"
};

if (personObject.age <= 40 && personObject.age >= 20) {
    window.alert("This person is between 20 and 40.");
}

else {
    window.alert("This person is not between 20 and 40.");
}